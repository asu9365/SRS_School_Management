# Example Users

Here is a list of the default user accounts and their credentials available in the project for testing purposes. All these test accounts use the same default password.

| Role | User ID (Email) | Password |
| :--- | :--- | :--- |
| **Super Admin** | `superadmin@schoolos.com` | `password` |
| **Admin** | `admin@schoolos.com` | `password` |
| **Teacher** | `teacher@schoolos.com` | `password` |
| **Student** | `student@schoolos.com` | `password` |

*Note: The database seeders also dynamically generate several other dummy users (students, teachers, parents) using the factory, and all of them also default to the password `password`.*
